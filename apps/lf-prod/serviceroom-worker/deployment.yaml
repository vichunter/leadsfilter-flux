apiVersion: apps/v1
kind: Deployment
metadata:
  name: serviceroom-worker
  namespace: lf-prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: serviceroom-worker
  template:
    metadata:
      labels:
        app: serviceroom-worker
    spec:
      enableServiceLinks: false
      imagePullSecrets:
        - name: ecr-credentials-leadsfilter
      containers:
        - name: serviceroom-worker
          image: 925375122018.dkr.ecr.us-east-2.amazonaws.com/serviceroom-backend:PLACEHOLDER # {"$imagepolicy": "flux-system:serviceroom-backend-prod"}
          command:
            - celery
            - --app=worker
            - worker
            - --concurrency=1
            - --hostname=worker@%h
            - --loglevel=INFO
          resources:
            requests:
              memory: 128Mi
              cpu: 50m
            limits:
              memory: 256Mi
          envFrom:
            - secretRef:
                name: serviceroom
